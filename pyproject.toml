[project]
name = "affordablehousingprice"
version = "0.1.0"
description = "根据人均可支配收入计算合理房价的Python模块与API服务"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
